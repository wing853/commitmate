<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>500 - 서버 오류</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Pretendard', sans-serif; }
        body {
            background-color: #0e0f11; color: #fff;
            display: flex; align-items: center; justify-content: center; height: 100vh;
        }
        .error-wrapper {
            text-align: center; padding: 48px;
            background: #111214;
            border: 0.5px solid rgba(248,113,113,0.15);
            border-radius: 20px; width: 90%; max-width: 460px;
        }
        .error-code {
            font-size: 96px; font-weight: 800; margin: 0;
            color: #f87171; letter-spacing: -4px; line-height: 1;
            opacity: 0.15;
        }
        .error-title { font-size: 20px; font-weight: 700; margin: 16px 0 12px; color: #fff; }
        .error-desc { color: #444; font-size: 14px; line-height: 1.7; margin-bottom: 36px; }
        .btn-home {
            background: #f87171; color: #fff;
            padding: 12px 28px; border-radius: 10px;
            font-weight: 700; font-size: 14px;
            text-decoration: none; display: inline-block; transition: 0.2s;
        }
        .btn-home:hover { background: #f95e5e; transform: translateY(-2px); }
    </style>
    <link rel="stylesheet" href="/css/app-theme.css">
</head>
<body>
<div class="error-wrapper">
    <h1 class="error-code">500</h1>
    <h2 class="error-title">서버에 문제가 생겼습니다</h2>
    <p class="error-desc">
        일시적인 오류가 발생하여 요청을 처리하지 못했습니다.<br>
        관리자에게 문의하거나 잠시 후 다시 시도해 주세요.
    </p>
    <a href="/" class="btn-home">홈으로 돌아가기</a>
</div>
</body>
</html>
